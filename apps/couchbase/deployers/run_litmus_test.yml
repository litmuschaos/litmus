---
apiVersion: batch/v1
kind: Job
metadata:
  generateName: litmus-couchbase-
  namespace: litmus
spec:
  template:
    metadata:
      name: litmus
      labels:
        app: couchbase-deployment-litmus

    spec:
      serviceAccountName: litmus
      restartPolicy: Never
      containers:
      - name: ansibletest
        image: openebs/ansible-runner:ci
        imagePullPolicy: Always

        env:
          - name: ANSIBLE_STDOUT_CALLBACK
            #value: log_plays
            value: default

          - name: PROVIDER_STORAGE_CLASS
            # Supported values: openebs-standard, local-storage
            value: openebs-cstor-sparse

          - name: APP_PVC
            value: couchbase-data

            # Application label
          - name: APP_LABEL
            value: 'app=couchbase'

            # Application namespace
          - name: APP_NAMESPACE
            value: app-couch-ns 

          - name: DEPLOY_TYPE
            value: statefulset

          # Use 'deprovision' for app-clean up
          - name: ACTION
            value: provision

        command: ["/bin/bash"]
        args: ["-c", "ansible-playbook ./couchbase/deployers/test.yml -i /etc/ansible/hosts -v; exit 0"]

