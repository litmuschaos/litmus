import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles((theme) => ({
	value: {
		textAlign: "center",
		paddingTop: 40,
		fontFamily: "Ubuntu",
		fontSize: 40,
		color: "#FFFFFF",
		fontWeight: 500,
	},
	statType: {
		textAlign: "center",
		fontFamily: "Ubuntu",
		fontSize: 18,
		color: "#FFFFFF",
	},
}));
