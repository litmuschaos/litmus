version: 2
jobs:
    build:
        branches:  
            ignore:   
                - master
        machine: true
        steps:
            - checkout

            # build the application image
            - run: |
                  echo "$DPASS" | docker login --username $DUNAME --password-stdin
                  cd litmus-portal && make build push