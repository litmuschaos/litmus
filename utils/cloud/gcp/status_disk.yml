- name: Getting disk users
  shell: gcloud compute disks describe {{ disk_name }} --zone {{ zone_name }} --format="csv(users)"
  register: disk_users

- include: disk_loss_ansible_prerequisites.yml

- include_vars:
    file: chaosutil.yml
   
- name: Checking disk is attached to node
  shell: echo "disk attached"   
  when: "inuse == true"