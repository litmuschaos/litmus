- name: "[Include]: Including the cstor_kill_and_verify_pool.yml"
  include: "cstor_kill_and_verify_pool.yml"
  loop: "{{ range(0, chaos_iterations|int, 1)|list }}"
  loop_control:
    loop_var: outer_item